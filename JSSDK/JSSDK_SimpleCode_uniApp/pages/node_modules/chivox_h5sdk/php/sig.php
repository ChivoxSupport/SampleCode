<?php
  header('Content-type: application/json;charset=utf-8');
  $alg_arr=['sha1','sha256','md5'];
  $alg=@$_POST['alg'];
  if($alg==NULL||!in_array($alg, $alg_arr)){
           $alg='sha1';
  }
  $appKey = "14255202120000cf";
  $secretKey = "988aaf63008b6db30f38bd60be0c3534";
  $timestamp=floor(microtime(1)*1000);
  $uid = "guest";
  $rs=json_encode(['timestamp'=>(string)$timestamp,'sig'=>hash($alg,$appKey . $timestamp . $secretKey)]);
  die($rs);
